import * as consts from 'consts';
import styled from 'styled-components';

export const SubTitle = styled.h2`
  color: ${consts.ui.colors.textSecondary};
  font-size: ${consts.ui.fontSizes.headerTitle};
  font-weight: 200;
`;
