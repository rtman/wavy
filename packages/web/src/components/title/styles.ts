import * as consts from 'consts';
import styled from 'styled-components';

export const Title = styled.h1`
  color: ${consts.ui.colors.textPrimary};
  font-size: ${consts.ui.fontSizes.bigTitle};
  font-weight: 200;
`;
