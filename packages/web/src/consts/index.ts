import * as localStorage from './localStorage';
import * as ui from './ui';
import * as routes from './routes';
import * as queries from './queries';
import * as mutations from './mutations';

export { ui, localStorage, mutations, routes, queries };
